# 프로젝트 구조 상세 설명

## 전체 디렉토리 구조

```
careerroadmap/
│
├── 📁 app/                           # Next.js 14 App Router (라우팅 및 페이지)
│   ├── 📁 (dashboard)/               # 그룹 라우트 (URL에 표시 안 됨)
│   │   ├── layout.tsx                # 대시보드 공통 레이아웃 (사이드바 포함)
│   │   ├── page.tsx                  # 메인 대시보드 ("/")
│   │   ├── 📁 roadmap/
│   │   │   └── page.tsx              # 로드맵 페이지 ("/roadmap")
│   │   ├── 📁 daily-quest/           # [예정]
│   │   │   └── page.tsx              # 데일리 퀘스트 ("/daily-quest")
│   │   ├── 📁 projects/              # [예정]
│   │   │   ├── page.tsx              # 프로젝트 목록 ("/projects")
│   │   │   └── [id]/                 # 동적 라우트
│   │   │       └── page.tsx          # 프로젝트 상세 ("/projects/123")
│   │   └── 📁 settings/              # [예정]
│   │       └── page.tsx              # 설정 페이지 ("/settings")
│   │
│   ├── globals.css                   # 전역 CSS (Tailwind 포함)
│   └── layout.tsx                    # 루트 레이아웃 (모든 페이지 공통)
│
├── 📁 components/                    # 재사용 가능한 React 컴포넌트
│   ├── 📁 ui/                        # Shadcn/UI 기본 컴포넌트
│   │   ├── card.tsx                  # Card 컴포넌트
│   │   ├── button.tsx                # Button 컴포넌트
│   │   └── progress.tsx              # Progress Bar 컴포넌트
│   │
│   ├── Sidebar.tsx                   # 사이드바 네비게이션
│   ├── Header.tsx                    # [예정] 헤더 컴포넌트
│   └── DailyGoalCard.tsx             # [예정] 일일 목표 카드
│
├── 📁 lib/                           # 유틸리티 및 헬퍼 함수
│   ├── supabase.ts                   # Supabase 클라이언트 인스턴스
│   ├── utils.ts                      # 공통 유틸리티 함수
│   └── types.ts                      # [예정] TypeScript 타입 정의
│
├── 📁 supabase/                      # Supabase 관련 파일
│   ├── schema.sql                    # 데이터베이스 스키마 정의
│   └── migrations/                   # [예정] 마이그레이션 파일
│
├── 📁 public/                        # 정적 파일 (이미지, 폰트 등)
│   ├── favicon.ico
│   └── images/
│
├── .env.example                      # 환경변수 예시 파일
├── .env.local                        # 실제 환경변수 (Git 무시됨)
├── .gitignore                        # Git 무시 파일 목록
├── next.config.js                    # Next.js 설정
├── package.json                      # 프로젝트 의존성
├── postcss.config.js                 # PostCSS 설정 (Tailwind용)
├── tailwind.config.ts                # Tailwind CSS 설정
├── tsconfig.json                     # TypeScript 설정
├── README.md                         # 프로젝트 소개
├── SETUP_GUIDE.md                    # 설치 가이드
└── STRUCTURE.md                      # 이 파일
```

## 주요 파일 설명

### 1. 라우팅 (app/)

#### `app/layout.tsx`
- 모든 페이지에 공통으로 적용되는 루트 레이아웃
- HTML, Body 태그를 포함
- 폰트 설정 (Inter)
- 메타데이터 정의

#### `app/(dashboard)/layout.tsx`
- 대시보드 페이지들의 공통 레이아웃
- Sidebar 컴포넌트 포함
- 왼쪽 사이드바 + 오른쪽 컨텐츠 영역 구조

#### `app/(dashboard)/page.tsx`
- 메인 대시보드 페이지
- 통계 카드 (목표 달성률, 로드맵 진행률 등)
- 최근 활동 로그
- 다가오는 일정
- 활동 잔디 (GitHub 스타일)

#### `app/(dashboard)/roadmap/page.tsx`
- 3년 로드맵 타임라인
- 학년별 목표 표시
- 목표 상태 관리 (예정/진행중/완료)

### 2. 컴포넌트 (components/)

#### `components/Sidebar.tsx`
- 왼쪽 사이드바 네비게이션
- 메뉴 아이템 (대시보드, 로드맵, 데일리 퀘스트, 프로젝트, 설정)
- 현재 페이지 하이라이트

#### `components/ui/card.tsx`
- Shadcn/UI Card 컴포넌트
- CardHeader, CardTitle, CardDescription, CardContent, CardFooter

#### `components/ui/button.tsx`
- Shadcn/UI Button 컴포넌트
- 다양한 variant (default, destructive, outline, secondary, ghost, link)
- 다양한 size (default, sm, lg, icon)

#### `components/ui/progress.tsx`
- Progress Bar 컴포넌트
- 진행률 시각화

### 3. 라이브러리 (lib/)

#### `lib/supabase.ts`
```typescript
// Supabase 클라이언트 생성
export const supabase = createClient(url, key)
```

#### `lib/utils.ts`
- `cn()`: 클래스명 병합 함수 (clsx + tailwind-merge)
- `formatDate()`: 날짜 포맷팅
- `calculateDDay()`: D-Day 계산

### 4. 데이터베이스 (supabase/)

#### `supabase/schema.sql`
- **users**: 사용자 프로필
- **roadmaps**: 학년별 로드맵
- **daily_goals**: 일일 목표
- **projects**: 프로젝트 포트폴리오

각 테이블에는 Row Level Security (RLS) 정책이 적용되어 있습니다.

## 데이터 흐름

### 예시: 대시보드 페이지

```
1. 사용자가 "/" 접속
   ↓
2. app/layout.tsx (루트 레이아웃)
   ↓
3. app/(dashboard)/layout.tsx (대시보드 레이아웃, Sidebar 포함)
   ↓
4. app/(dashboard)/page.tsx (대시보드 컨텐츠)
   ↓
5. Supabase에서 데이터 가져오기 (lib/supabase.ts)
   ↓
6. UI 컴포넌트로 렌더링 (components/ui/*)
```

## Next.js 14 App Router 특징

### 1. 파일 기반 라우팅
- `page.tsx`: 해당 경로의 UI
- `layout.tsx`: 공유 레이아웃
- `loading.tsx`: 로딩 UI
- `error.tsx`: 에러 UI
- `not-found.tsx`: 404 페이지

### 2. 그룹 라우트 `(dashboard)`
- URL에 영향을 주지 않고 파일만 그룹화
- 공통 레이아웃 적용

### 3. 동적 라우트 `[id]`
- `[id]` 폴더: 동적 경로 매개변수
- 예: `/projects/123`에서 `123`은 `params.id`로 접근

## 스타일링 구조

### Tailwind CSS 설정

```typescript
// tailwind.config.ts
theme: {
  extend: {
    colors: {
      primary: 'Royal Blue',
      secondary: 'Mint Green',
      // ...
    }
  }
}
```

### CSS 변수 (globals.css)

```css
:root {
  --primary: 221.2 83.2% 53.3%;
  --secondary: 160 84% 39%;
  /* ... */
}
```

## 타입 안정성

모든 컴포넌트는 TypeScript로 작성되어:
- 자동 완성
- 타입 체크
- 런타임 오류 방지

## 보안

### Row Level Security (RLS)
- 사용자는 자신의 데이터만 접근 가능
- SQL 레벨에서 권한 제어

### 환경변수
- `.env.local`은 Git에서 무시됨
- API 키는 클라이언트에 노출되지 않음

## 성장 로드맵

### Phase 1: 기본 기능 (현재)
- ✅ 프로젝트 구조
- ✅ 대시보드
- ✅ 로드맵

### Phase 2: 핵심 기능
- ⬜ 데일리 퀘스트
- ⬜ 프로젝트 아카이브
- ⬜ 인증 시스템

### Phase 3: 고급 기능
- ⬜ 소셜 로그인
- ⬜ 이미지 업로드
- ⬜ 알림 기능

### Phase 4: 배포
- ⬜ Vercel 배포
- ⬜ 도메인 연결
- ⬜ SEO 최적화
